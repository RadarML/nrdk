objectives: []

encoder:
  name: UNetEncoder
  args:
    dim: 64
    n_doppler: 64

decoders: []

optimizer:
  name: AdamW
  default_lr: 0.0001
  warmup: 100
  subsets: {}

dataset:
  batch_size: 16
  channels:
    radar:
      name: RawChannel
      indices: radar
      args:
        sensor: radar
        channel: iq
        transform:
        - name: DiscardTx2
          args: {}
        - name: IIQQtoIQ
          args:
            scale: 0.001
        - name: FFTLinear
          args:
            pad: 56
            axes: [0, 1, 2]
        - name: ComplexAmplitude
          args: {}
