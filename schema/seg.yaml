metrics:
  seg_loss: Loss (CE)
  seg_acc: Accuracy
  seg_top2: Top-2 Accuracy
  seg_miou: mIOU

splits:
  all: ""
  indoor: ^indoor
  outdoor: ^outdoor
  bike: ^bike

methods:
  segment:
    name: Segmentation
    methods: [
      "seg.s/p10", "seg.s/p20", "seg.s/p50", "seg.s/base",
      "seg.s/t10", "seg.s/t20", "seg.s/t50", "seg.s/t100"
    ] 
    descriptions: [
      "10% Trained", "20% Trained", "50% Trained", "100% Trained",
      "10% Tuned", "20% Tuned", "50% Tuned", "100% Tuned",
    ]
