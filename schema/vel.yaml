metrics:
  vel_loss: Velocity
  vel_speed: Speed (Doppler Bins)
  vel_speedp: Speed (%)
  vel_angle: Angle (Degrees)

splits:
  all: ""
  indoor: ^indoor
  outdoor: ^outdoor
  bike: ^bike

methods:
  vel:
    name: Velocity
    methods: ["vel/p10", "vel/p20", "vel/p50", "vel/base"] 
    descriptions: ["10% Trained", "20% Trained", "50% Trained", "100% Trained"]
